let main4 = {
    title : ['Requirements','Local','Docker'],
    subtitle : [`It’s also possible to run the app using Docker and Docker Compose. It can be done by running the commands below.`]
}
export default main4;